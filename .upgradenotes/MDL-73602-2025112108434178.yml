issueNumber: MDL-73602
notes:
  core:
    - message: >-
        Removed `qtype_random` from core. `random` is no longer a valid name for
        a `qtype` plugin, to ensure we can continue to support restoring
        historic backups.
      type: removed
